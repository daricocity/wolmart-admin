const config = {
    basename: '',
    defaultPath: '/',
    fontFamily: `'Poppins', sans-serif`,
    borderRadius: 12,
    proxy: 'https://wolmart-api.onrender.com/api',
    // proxy: 'https://wolmart-api.herokuapp.com/api'
};

export default config;
